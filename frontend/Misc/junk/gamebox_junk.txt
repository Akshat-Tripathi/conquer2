 <Box className="gamebox-container">
      <h1 className="gamebox-title">Join the game!</h1>
      <Box alignItems="center">
        <form classname="gamebox-form">
          <div>
            <FormControl variant="filled" className="ign-input">
              <label>Username</label>
              <TextField
                required
                id="outlined-required"
                label="Required"
                placeholder="Genghis Khan"
                variant="filled"
                align="center"
                color="white"
              />
            </FormControl>
          </div>
          <div>
            <FormControl variant="filled" className="gamemode-input">
              <label>Gamemode</label>
              <TextField
                id="standard-select-currency"
                select
                label="Select"
                value={war}
                onChange={handleChange}
                helperText="Choose War Event"
                variant="filled"
              >
                {gamemodes.map((option) => (
                  <MenuItem key={option.value} value={option.value}>
                    {option.label}
                  </MenuItem>
                ))}
              </TextField>
            </FormControl>
          </div>

          <Button variant="outlined" color="secondary" align="center">
            Commence War
          </Button>
        </form>
      </Box>
    </Box>